package by.epam.sak.homework1.task9;

class Calculate {
    static double getCircleLength(double radius) {
        return  2 * radius * Math.PI;
    }
    static double getCircleArea(double radius) {
        return Math.pow(radius, 2) * Math.PI;
    }
}
